name: publish

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      yank_version:
        description: "Crate version to yank from crates.io"
        required: true
        type: string

permissions:
  contents: read
  id-token: write

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: release

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Check version
        shell: bash
        run: |
          crate_version="$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[] | select(.name == "incrededup") | .version')"
          expected_version="${GITHUB_REF_NAME#v}"
          if [ "$crate_version" != "$expected_version" ]; then
            echo "Cargo.toml version $crate_version does not match release version $expected_version" >&2
            exit 1
          fi

      - name: Format
        run: cargo fmt -- --check

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: CLI docs are generated
        run: |
          cargo run --example generate_cli_docs
          git diff --exit-code README.md docs/cli.md

      - name: Test
        run: cargo test --all-targets

      - name: Doctest
        run: cargo test --doc

      - name: Publish dry run
        run: cargo publish --dry-run --locked

      - name: Authenticate with crates.io
        id: auth
        uses: rust-lang/crates-io-auth-action@v1

      - name: Publish
        run: cargo publish --locked
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}

  yank:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: release

    steps:
      - uses: dtolnay/rust-toolchain@stable

      - name: Validate version
        shell: bash
        env:
          YANK_VERSION: ${{ inputs.yank_version }}
        run: |
          if [[ ! "$YANK_VERSION" =~ ^[0-9]+[.][0-9]+[.][0-9]+$ ]]; then
            echo "Refusing to yank invalid version: $YANK_VERSION" >&2
            exit 1
          fi

      - name: Authenticate with crates.io
        id: auth
        uses: rust-lang/crates-io-auth-action@v1

      - name: Yank version
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
          YANK_VERSION: ${{ inputs.yank_version }}
        run: cargo yank --version "$YANK_VERSION" incrededup
